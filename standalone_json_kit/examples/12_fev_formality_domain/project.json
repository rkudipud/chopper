{
  "$schema": "chopper/project/v1",
  "project": "PROJECT_FEV_FULL",
  "domain": "fev_formality",
  "owner": "fev-team",
  "release_branch": "main",
  "base": "fev_formality/chopper/base.json",
  "features": [
    "fev_formality/chopper/features/fev_fm_lite.json",
    "fev_formality/chopper/features/fev_fm_eco.json"
  ],
  "notes": [
    "fev_fm_lite: no dependencies — applies to any base with fev_fm_rtl2rtl stage",
    "fev_fm_eco: no inter-feature dependency, but requires fev_fm_rtl2gate and fev_fm_rtl2rtl stages to exist in the compiled base",
    "Both features are independent; lite before eco is conventional but not required by depends_on"
  ]
}
