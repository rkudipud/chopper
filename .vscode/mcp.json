{
	"inputs": [

	],
	"servers": {
		"mempalace": {
			"type": "stdio",
			"command": "${workspaceFolder}/.venv/Scripts/python.exe",
			"cwd": "${workspaceFolder}",
			"args": [
				"-m",
				"mempalace.mcp_server",
				"--palace",
				"${workspaceFolder}"
			]
		}
	}
}