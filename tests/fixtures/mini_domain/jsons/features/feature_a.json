{
  "$schema": "chopper/feature/v1",
  "name": "feature_a",
  "description": "Test feature that adds a proc via procedures.include and exercises same-source procedures.exclude PROC_TRIM pruning",
  "files": {
    "include": [
      "extra_utils.tcl"
    ]
  },
  "procedures": {
    "include": [
      {
        "file": "main_flow.tcl",
        "procs": ["cleanup_flow"]
      }
    ],
    "exclude": [
      {
        "file": "helper_procs.tcl",
        "procs": ["debug_dump"]
      }
    ]
  }
}
