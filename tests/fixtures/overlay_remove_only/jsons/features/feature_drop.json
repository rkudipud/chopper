{
  "$schema": "feature-v1",
  "name": "feature_drop",
  "description": "Removes core.tcl from the running set. Emits VW-21 (layer-shadowed, action=remove).",
  "files": {
    "exclude": [
      "core.tcl"
    ]
  }
}
